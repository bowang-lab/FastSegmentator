Refactoring Log — nnunet_infer_nii.py
Date: 2026-02-23
=======================================

## Imports
- Removed duplicate imports: `tqdm` (imported twice), `os` (imported twice)
- Grouped imports by stdlib -> third-party -> local (PEP 8)
- Merged two `label_handling` import lines into one

## Bug Fixes
- `files[1:]` changed to `files` — was silently skipping the first input file
- `--use_softmax` argument changed to `action='store_true'` so it works as a
  proper boolean flag (previously accepted a string, which was always truthy)
- Removed redundant `load_state_dict(parameters[0])` call before the loop that
  already loads all parameters including index 0

## Debug Artifacts Removed
- `print(self.network)` — dumped the entire network architecture on every init
- `print(image.shape)` in `inference()` method
- Commented-out `#data = torch.from_numpy(data)...` line in `preprocess()`
- Misleading comment `# Start time for inference time calculation` on empty_cache

## Code Quality Improvements
- Fixed misaligned comment block inside `logits_to_segmentation`
- Extracted repeated expressions into local variables (`target_shape`, `original_spacing`)
- Used `os.path.basename()` instead of fragile `split('/')` for case_name
- Used `checkpoint.get(key)` instead of manual `if key in dict.keys()` check
- Used `'nnUNet_compile' in os.environ` instead of `os.environ.keys()`
- Formatted timing output with `:.2f` for readability
- Consistent indentation, removed excessive blank lines and trailing whitespace
- Added type hint `use_softmax: bool` to function signature
